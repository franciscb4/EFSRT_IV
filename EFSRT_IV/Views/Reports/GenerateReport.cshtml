@model List<EFSRT_IV.Models.Sell>

@{
    ViewData["Title"] = "Generar Reportes";
}

<h1>Generar Reportes</h1>
<hr />

<form asp-controller="Reports" asp-action="GenerateReport" method="post">
    @* <div class="form-group">
        <label for="format">Selecciona Formato del Reporte:</label>
        <select id="format" name="format" class="form-control" required>
            <option value="">-- Selecciona --</option>
            <option value="excel">Excel</option>
            <option value="pdf">PDF</option>
            <option value="csv">CSV</option>
        </select>
    </div> *@
    <div>

    </div>

    <button type="submit" class="btn btn-primary">Descargar Reporte</button>
</form>


<div class="w-100">
    <div class="row ps-0">
        @foreach(var item in Model)
        {
            <div class="card px-0 my-2">
                <div class="card-header">
                    Fecha @item.date.ToShortDateString()
                </div>
                <div class="card-body">
                    <h5 class="card-title">Resumen</h5>
                    <p class="card-text">Cantidad de productos vendidos: 123</p>
                    <p class="card-text">Cantidad de ventas realizadas: 123</p>
                </div>
                <div class="card-footer">
                    <a href="#" class="btn btn-primary">PDF</a>
                </div>
            </div>
        }
    </div>
</div>

<div id="sideBar">
</div>
@section Scripts {
    <script>
        $("#sideBar").load("/Store/SideBar")
    </script>
}